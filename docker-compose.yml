services:
  app:
    build:
      dockerfile: flask.Dockerfile
    environment:
      - RUNTIME=develop
      - SECRET_KEY=abc123
    ports:
      - 5001:8080
    volumes:
      - ./:/app
  dev:
    image: ghcr.io/nationalarchives/tna-python-dev:preview
    volumes:
      - ./:/app
